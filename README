
needs an array of files implemented for real use
needs folder structures for each pdf document to house multiple pages correctly
possible to implement as a linux if needed for script
Does not convert PDF to images to use OCR so this will not work if there is no text in the pdf
